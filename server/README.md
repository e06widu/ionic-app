## Install dependamcies 

`npm install`

## run the application for dev mode
# install nodemon globally
`npm install -g nodemon`
# run the app locally
`nodemon app.js` 

## Sample data 
`/data` folder has all the data json files
